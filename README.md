# Python Quiz Program

This Python program runs a short quiz by reading multiple-choice questions from a file and storing them using a Question class. Each question is shown to the user, who selects an answer, and the program tracks the score. The Question class includes methods to access and modify question data and formats questions for display using the __str__ method.








